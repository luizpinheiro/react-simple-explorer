import styled from 'styled-components'

export const ClickLabel = styled.button`
  border: none;
  margin: 0;
  padding: 0;
  background: transparent;

  &:hover {
    text-decoration: underline;
  }
`

export const IconContainer = styled.span`
  margin-left: 5px;
`
